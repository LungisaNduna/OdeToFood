@model IEnumerable<OdeToFood.Data.Models.Restaurant>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@foreach(var restaurant in Model)
{
    @Html.Partial("_RestaurantSummary", restaurant)
}

@section footer {
    <div>We do not rate or get paid by Restaurants. We just list them.</div>
}
